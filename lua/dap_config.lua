-- local dbg_list = require("dap-install.debuggers_list").debuggers
-- use this to know the list of available debuggers
O.dap = {
  breakpoint = {
    text = "",
    texthl = "LspDiagnosticsSignError",
    linehl = "",
    numhl = "",
  },
  lang = {
    ruby_vsc_dbg = {}, -- ruby debugger with no extra conf
  },
}
